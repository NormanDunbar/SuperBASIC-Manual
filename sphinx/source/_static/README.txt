This folder must exist for the build. There shouldn't [really]
be any files in it, but having it here means an error from the
build no longer occurs.

However, when building epubs, there will be a warning, but 
that's only because of *this* file. Sigh.

Norman Dunbar
4 November 2025.
